# GoEn バグ修正・機能追加 (2026-09-17)

## 🔴 P1 ブロッカー

### Issue 1: AI が動かない
**症状**: Cloud Functions 呼び出し失敗またはタイムアウト
**原因推定**: 
- Firebase google_app_id 未設定
- Cloud Functions エンドポイント未配置
- Go エンジンサービス例外処理

**修正方針**:
```dart
// lib/services/go_engine_service.dart
- Firebase 認証トークン確認
- Cloud Functions URL バリデーション
- リトライロジック改善（指数バックオフ）
- エラーログ充実化
```

**テスト**:
```bash
flutter test test/unit/services/go_engine_service_test.dart -v
```

---

### Issue 2: 碁が線上にない
**症状**: 石がグリッド交点からズレて描画される
**原因推定**:
- 座標変換計算エラー（サイズ計算）
- DPR（デバイスピクセル比）非対応
- CustomPainter 座標系ズレ

**修正方針**:
```dart
// lib/views/widgets/go_board_painter.dart (未確認)
// 座標計算
double stoneX = padding + (col + 0.5) * cellSize;
double stoneY = padding + (row + 0.5) * cellSize;

// 検証: padding + cellSize * boardSize = 画面幅
```

**確認コマンド**:
```bash
# ファイルを探す
find lib -name "*painter*" -o -name "*board*" -type f

# 座標計算をログ出力
flutter run --debug
```

---

### Issue 3: UI デザイン和風化
**機能追加**: 日本的なデザイン要素を統合

**実装方針**:
```dart
// lib/config/theme.dart に追加
- 墨色（#2B2B2B）ベース
- 和紙テクスチャ背景
- 鰺の子絞り（矢絣）パターン
- 書体：Google Fonts でセリフ選択
- 欄干や唐草模様の UI トリム

// Color palette
const primaryJapanese = Color(0x2B2B2B);  // 墨
const accentGold = Color(0xD4AF37);       // 金
const bambooGreen = Color(0x008000);      // 竹色
```

---

## 📋 実装優先度

| # | タスク | 難易度 | 所要時間 |
|----|--------|--------|---------|
| 1 | Firebase google_app_id 再設定 | ⭐ | 15分 |
| 2 | Cloud Functions デプロイ確認 | ⭐⭐ | 30分 |
| 3 | Go エンジン Go Engine Service ログ追加 | ⭐ | 20分 |
| 4 | 石の座標計算デバッグ | ⭐⭐ | 45分 |
| 5 | 和風テーマ実装 | ⭐⭐⭐ | 2時間 |

---

## 🔍 ファイル構造確認

```bash
# 実行
cd /home/user/goen
find lib/views -name "*.dart" | grep -i "board\|painter\|stone"
grep -r "stoneRadius\|stoneX\|stoneY" lib/
```

---

**次セッション開始時**: 
1. Firebase 再設定
2. AI ログ追加・デバッグ
3. 座標計算修正
4. 和風デザイン実装
