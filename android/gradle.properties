org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false

# Android Signing Configuration
# Load from environment variables (recommended for CI/CD)
# or gradle.properties (local development only)
# NEVER commit sensitive passwords to version control
ANDROID_KEYSTORE_PATH=app/goen-release.keystore
ANDROID_KEY_ALIAS=goen-key
